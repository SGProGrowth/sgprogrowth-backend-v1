# Environment variables (frontend)

# REST API base URL — must match backend API_PREFIX (default: api/v1)
# Local development:
VITE_API_URL=http://localhost:3000/api/v1

# Production example:
# VITE_API_URL=https://api.example.com/api/v1
